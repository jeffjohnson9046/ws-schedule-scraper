package main

import "testing"

// TODO: write real tests; this is here just to kick off the program such that it can be debugged.
func TestMain(t *testing.T) {
	t.Run("test", func(t *testing.T) {
		main()
	})
}
